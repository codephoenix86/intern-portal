# Client documentation

Documentation for the Career Navigator React (Vite) client lives in this folder.

| Document | Contents |
|----------|----------|
| [Architecture](./architecture.md) | Tech stack, app entry, providers, auth, HTTP layer, `src/` folder map |
| [Routing](./routing.md) | Top-level and nested routes, pages, and **where each screen loads data** |
| [Components & services](./components-and-services.md) | UI modules by folder, service ↔ API map, context/hooks, API vs mock summary |

**Quick start** (install, env, scripts) stays in the [client README](../README.md) at the repository root of the client package.
